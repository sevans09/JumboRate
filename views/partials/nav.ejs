<!-- nav.ejs -->
<!-- Navigation Bar for all .ejs (html) pages -->

<nav class="navbar navbar-expand-lg navbar-dark sticky-top" style="background-color: rgb(35, 94, 164)">
    <a class="navbar-brand head" href="home" style="font-size: 24px">Jumbo Rate</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
                <a class="nav-link" href="search">Search for a Class</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="rate">Rate a Class</a>
            </li>
        </ul>
        <div id="display_name" style="color: rgba(255,255,255,.5)"></div>
        <ul class="navbar-nav my-2 my-lg-0">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img src="profile_icon.png" style="height:25px;" >
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item brown" href="#" id="login">Log In</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item brown" href="#" id="signout">Sign Out</a>
                </div>
            </li>
        </ul>
    </div>
</nav>

<script>
    firebase.auth().onAuthStateChanged(user => {
        if (user) {
            var name_container = document.getElementById("display_name");
            var name = document.createElement('a');
            name.innerHTML = "Hi, " + user.displayName + "!";
            name_container.classList.add("nav-link");
            name_container.appendChild(name);
        }
    });
</script>
